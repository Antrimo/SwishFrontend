import 'package:flutter/material.dart';

class StackBlock extends StatelessWidget {
  const StackBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}